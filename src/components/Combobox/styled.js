import { makeStyles } from '@material-ui/core/styles';
import { colorWhite } from '../../assets/styles/colors'


export const useStyles = makeStyles(() => ({
    formControl: {
        width: '100%'
    },
    selectBox: {
        height: 50,
        backgroundColor: colorWhite
    }
}))
